#!/bin/bash

echo "We can create .txt file, add there some code that might be executable, then change through file manager its properties, by changing extension from .txt to .sh (for example), and also put a mark in front of "allow file be executable". Then we can run ./filename"
